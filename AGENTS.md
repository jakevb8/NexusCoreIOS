# NexusCoreIOS — Agent Instructions

## Project Overview

NexusCoreIOS is the SwiftUI/iOS native client for NexusCore Resource Management SaaS. It connects to either the NexusCoreJS (Node/NestJS) or NexusCoreDotNet (ASP.NET Core) REST APIs via a user-selectable backend toggle. The project is generated with **xcodegen** and uses **Swift Package Manager** for Firebase dependencies (no CocoaPods).

## Sister Repos

| Repo             | Path                                    | GitHub                     | Purpose                                                 |
| ---------------- | --------------------------------------- | -------------------------- | ------------------------------------------------------- |
| NexusCoreJS      | `/Users/jake/projects/NexusCore`        | `jakevb8/NexusCore`        | Next.js/NestJS fullstack (source of truth for features) |
| NexusCoreDotNet  | `/Users/jake/projects/NexusCoreDotNet`  | `jakevb8/NexusCoreDotNet`  | ASP.NET Core 8 Razor Pages fullstack                    |
| NexusCoreAndroid | `/Users/jake/projects/NexusCoreAndroid` | `jakevb8/NexusCoreAndroid` | Jetpack Compose Android client                          |
| NexusCoreReact   | `/Users/jake/projects/NexusCoreReact`   | `jakevb8/NexusCoreReact`   | Expo React Native cross-platform client                 |
| NexusCoreAngular | `/Users/jake/projects/NexusCoreAngular` | `jakevb8/NexusCoreAngular` | Angular 21 web client                                   |

**Feature parity rule:** All UI-facing changes must be propagated to all client apps (Android, iOS, React Native) in the same session. Backend-only changes (migrations, business logic) do not require client updates.

**Cross-repo check rule:** At the start of every task, read the relevant files in all sister repos to check whether an equivalent change has already been made there. If it has, apply the same change here. If this repo is ahead, propagate to the others. Never assume parity — always verify by reading the files.

## NEVER COMMIT SECRETS

Files that must NEVER be committed:

- `GoogleService-Info.plist` — Firebase config with API keys (gitignored)
- `*.p12`, `*.p8`, `*.pem` — signing certificates/keys
- Any file containing real API keys, private keys, or credentials

**Restore `GoogleService-Info.plist`:**

```bash
firebase apps:sdkconfig IOS 1:797114794124:ios:2e6c4eebb1fc19f4663ba9 --project nexus-core-rms --out NexusCoreIOS/GoogleService-Info.plist
```

## Project Setup

### Requirements

- Xcode 16+
- xcodegen (`brew install xcodegen`) v2.45.2+
- Firebase CLI (`npm install -g firebase-tools`)

### First-time setup

```bash
# 1. Generate the Xcode project
xcodegen generate

# 2. Restore GoogleService-Info.plist (gitignored)
firebase apps:sdkconfig IOS 1:797114794124:ios:2e6c4eebb1fc19f4663ba9 --project nexus-core-rms --out NexusCoreIOS/GoogleService-Info.plist

# 3. Open in Xcode — SPM will resolve Firebase + GoogleSignIn automatically
open NexusCoreIOS.xcodeproj

# 4. Replace placeholders in project.yml / Info.plist with real values from GoogleService-Info.plist:
#    - GIDClientID → CLIENT_ID value
#    - PLACEHOLDER_REVERSED_CLIENT_ID → REVERSED_CLIENT_ID value
# Then re-run: xcodegen generate
```

### Regenerate project after schema changes

```bash
xcodegen generate
```

Always re-run `xcodegen generate` after editing `project.yml`. Do NOT manually edit the `.xcodeproj` — it is a generated artifact.

## Firebase Registration

- **Project:** `nexus-core-rms` (number `797114794124`)
- **App ID:** `1:797114794124:ios:2e6c4eebb1fc19f4663ba9`
- **Bundle ID:** `me.jakev.nexuscore`
- **Auth:** Google sign-in only (no Email/Password)

## Architecture

```
NexusCoreIOS/
├── project.yml                         — xcodegen spec (SPM deps, build settings)
├── NexusCoreIOS/
│   ├── NexusCoreIOSApp.swift           — @main entry, FirebaseApp.configure()
│   ├── ContentView.swift               — Auth gate (loading / LoginView / MainNavigationView)
│   ├── Info.plist                      — Generated by xcodegen; GIDClientID + CFBundleURLSchemes
│   ├── Auth/
│   │   └── AuthState.swift             — ObservableObject wrapping Firebase auth listener
│   ├── Models/
│   │   └── Models.swift                — All Swift types, enums, DTOs
│   ├── Services/
│   │   ├── BackendPreference.swift     — UserDefaults-backed backend choice (JS vs .NET)
│   │   ├── APIClient.swift             — URLSession HTTP client, Bearer token, multipart
│   │   └── NexusAPI.swift              — All endpoint functions
│   └── Views/
│       ├── Theme.swift                 — Color constants + hex Color extension
│       ├── Components.swift            — StatusChipView, NexusCard, NexusButton, AppHeaderView
│       ├── Navigation/
│       │   └── MainNavigationView.swift — NavigationStack + AppRoute enum
│       ├── Login/
│       │   ├── LoginView.swift
│       │   ├── OnboardingView.swift
│       │   └── PendingApprovalView.swift
│       ├── Dashboard/
│       │   └── DashboardView.swift
│       ├── Assets/
│       │   ├── AssetsView.swift
│       │   └── AssetDetailView.swift
│       ├── Team/
│       │   └── TeamView.swift
│       ├── Reports/
│       │   └── ReportsView.swift
│       ├── Events/
│       │   └── EventsView.swift
│       └── Settings/
│           └── SettingsView.swift
└── NexusCoreIOSTests/
    └── ModelsTests.swift               — XCTest unit tests for models, enums, decoders
```

## Unit Tests

The project has an `NexusCoreIOSTests` unit test target. Tests live in `NexusCoreIOSTests/` and are run from Xcode (Product → Test, or `⌘U`) or via `xcodebuild`:

```bash
# Run tests from command line (requires Xcode + simulator)
xcodebuild test \
  -project NexusCoreIOS.xcodeproj \
  -scheme NexusCoreIOSTests \
  -destination 'platform=iOS Simulator,name=iPhone 16'
```

**What is tested** (`ModelsTests.swift`):
- `AssetStatus` raw values, display names, colors, `CaseIterable` count, `Decodable`
- `Role` raw values and `CaseIterable` count
- `BackendChoice` base URLs and labels
- `PaginatedEvents` resolving JS meta vs .NET flat fields and empty defaults
- `KafkaEvent` JSON decoding
- `Asset` JSON decoding

**Rules for new tests:**
- Keep tests in `NexusCoreIOSTests/` — one file per logical domain.
- Tests must not import Firebase (it is not linked to the test target). Test only pure Swift model/logic code.
- After adding tests, always run `xcodegen generate` if you added a new source file to the test target.

## Key Architecture Decisions

- **xcodegen + SPM**: No CocoaPods. `project.yml` is the source of truth. `.xcodeproj` is a generated artifact — always regenerate after schema changes.
- **NavigationStack**: Uses `NavigationPath` with a typed `AppRoute` enum. `MainNavigationView` owns the path and wires all `navigationDestination` handlers.
  - Views that push further (Assets, AssetDetail, Settings, Dashboard) receive `@Binding var path: NavigationPath` and append to it.
  - Leaf views (Team, Reports) use `@Environment(\.dismiss)` for back navigation.
- **AuthState**: `@EnvironmentObject` injected at the root. Pre-auth flow (Login → Onboarding → PendingApproval) is handled by `ContentView` reading `AuthState.status`.
- **APIClient**: Singleton, lazily created. Call `APIClient.reset()` after switching backends. Bearer token is fetched from `Auth.auth().currentUser?.getIDToken()` on each request.
- **Backend selector**: `BackendPreference` persists choice in `UserDefaults`. Two backends:
  - `JS` → `https://nexus-coreapi-production.up.railway.app/api/v1`
  - `DOTNET` → `https://nexuscoredotnet-production.up.railway.app/api/v1`

## Placeholders (must be replaced before building)

In `project.yml` (and regenerated `Info.plist`):

- `GIDClientID: PLACEHOLDER_CLIENT_ID` → Replace with `CLIENT_ID` from `GoogleService-Info.plist`
- `CFBundleURLSchemes: [PLACEHOLDER_REVERSED_CLIENT_ID]` → Replace with `REVERSED_CLIENT_ID` from `GoogleService-Info.plist`

## Canonical UI Feature List

| Screen       | Features                                                                                                                                     |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Login        | Google sign-in, backend selector (JS vs .NET), persisted choice                                                                              |
| Onboarding   | Display name + org name, POST /auth/register                                                                                                 |
| Pending      | Pending approval message, sign out                                                                                                           |
| Dashboard    | Navigation cards to Assets, Team, Reports, Events, Settings                                                                                  |
| Assets       | List with search + pagination, create/edit/delete (manager only), CSV import (.fileImporter), CSV sample download (UIActivityViewController) |
| Asset Detail | Name, SKU, description, status chip selector, assignedTo                                                                                     |
| Team         | List members, invite by email (sheet), copy-link fallback, remove member, change role                                                        |
| Reports      | Total assets, utilization %, assets-by-status bar chart                                                                                      |
| Events       | Paginated Kafka asset status change history (asset name, old/new status, timestamp)                                                          |
| Settings     | Account info, backend picker, sign out                                                                                                       |

## Common Pitfalls

- **LSP errors before SPM resolves**: After `xcodegen generate`, the LSP will show "No such module 'FirebaseAuth'" etc. until Xcode opens the project and SPM fetches packages. These are NOT real errors. Always verify by building in Xcode.
- **Never edit `.xcodeproj` directly**: It is regenerated by xcodegen. All project settings live in `project.yml`.
- **`GoogleService-Info.plist` is gitignored**: Every developer must restore it manually. See restore command above.
- **`AppHeaderView` `onBack` pattern**: Pass `{ dismiss() }` for leaf views, `{ path.removeLast() }` for views that own a path binding.
- **`FirebaseApp.configure()`** must be called in `NexusCoreIOSApp.swift` before any Firebase usage. It reads `GoogleService-Info.plist` at runtime.
- **`CFBundleURLSchemes`** must contain the `REVERSED_CLIENT_ID` from `GoogleService-Info.plist` for Google Sign-In callback to work.
- **Google Sign-In flow**: `GIDSignIn.sharedInstance.signIn(withPresenting: rootVC)` → `GoogleAuthProvider.credential(withIDToken:accessToken:)` → `Auth.auth().signIn(with:)`. The Firebase auth state listener in `AuthState` drives navigation automatically.
- **Deployment target**: iOS 16.0 minimum (required for `NavigationStack` + `NavigationPath`).
- After completing any task that modifies files, always commit and push to the current branch without asking for confirmation.
- **README maintenance**: After any feature addition, removal, or significant change, update `README.md` in every affected repo to reflect the current feature set, API table, tech stack, and test count. The READMEs are the public-facing source of truth and must not fall behind the code.
